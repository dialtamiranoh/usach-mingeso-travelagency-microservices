spring:
  application:
    name: ms-seguimiento
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
  datasource:
    url: jdbc:postgresql://localhost:5432/ta_ms_seguimiento
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
