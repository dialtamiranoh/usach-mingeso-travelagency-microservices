apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ms-reportes-pvc
  namespace: travelagency
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-reportes-postgres
  namespace: travelagency
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-reportes-postgres
  template:
    metadata:
      labels:
        app: ms-reportes-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_DB
              value: "ta_ms_reportes"
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: postgres-data
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: ms-reportes-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: ms-reportes-postgres
  namespace: travelagency
spec:
  selector:
    app: ms-reportes-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
