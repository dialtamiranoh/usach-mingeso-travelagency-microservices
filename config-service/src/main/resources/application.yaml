server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/dialtamiranoh/usach-mingeso-travelagency-microservices
          search-paths: config-data
  application:
    name: config-service
